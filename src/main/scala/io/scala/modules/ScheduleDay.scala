package io.scala.modules

import org.scalajs.dom

object ScheduleDay {
  def openDay(cityName: String): Unit = ???
    // dom.document.querySelectorAll(".tab button").foreach{elem =>
    //   elem.
    // }
}
