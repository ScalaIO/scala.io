{
  "name": "scalaio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "install": "npm run build",
    "start": "npx serve -l 8080 dist",
    "dev": "npx vite",
    "build": "npx sass src/main/resources/css/main.scss src/main/resources/css/output.scss && sbt \"compile; fullLinkJS\" && npx vite build",
    "preview": "npx vite preview"
  },
  "devDependencies": {
    "sass": "^1.56.1",
    "typescript": "4.6.2",
    "vite": "^2.9.15",
    "vite-plugin-html": "^3.2.0"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}